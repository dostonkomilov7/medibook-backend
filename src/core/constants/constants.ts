export enum UserStatus {
    active = 'Active',
    inactive = 'Inactive',
}

export enum DoctorStatus {
    active = 'Active',
    inactive = 'inactive',
    suspended = 'Suspended'
}

export enum UserRole {
    admin = 'Admin',
    doctor = 'Doctor',
    user = 'User',
}

export enum AppointmentStatus {
    PENDING = 'Pending',
    CONFIRMED = 'Confirmed',
    CANCELLED = 'Cancelled',
    COMPLETED = 'Completed',
}

